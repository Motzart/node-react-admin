"use strict";
const main = require("./main");
const profile = require("./profile");
const users = require("./users");
const posts = require("./posts");
const auth = require("./auth");

module.exports = { main, profile, users, posts, auth };
